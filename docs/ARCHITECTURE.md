# System Architecture
Internet -> Cloudflare (SSL/CDN) -> NGINX -> FastAPI -> PostgreSQL + Redis
                                              -> Prometheus + Grafana

Components: Cloudflare, NGINX, FastAPI, PostgreSQL, Redis, Prometheus, Grafana
